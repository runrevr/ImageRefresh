console.log = function() { const args = Array.from(arguments); const line = args.map(arg => typeof arg === 'object' ? JSON.stringify(arg) : arg).join(' '); require('fs').appendFileSync('./logs/server.log', line + '\n'); console.info.apply(console, arguments); }; console.error = function() { const args = Array.from(arguments); const line = args.map(arg => typeof arg === 'object' ? JSON.stringify(arg) : arg).join(' '); require('fs').appendFileSync('./logs/server.log', line + '\n'); console.info.apply(console, arguments); };
