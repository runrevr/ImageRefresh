let originalConsole = console.log; console.log = function() { const args = Array.from(arguments); const line = args.map(arg => typeof arg === 'object' ? JSON.stringify(arg) : String(arg)).join(' '); require('fs').appendFileSync('./logs/app.log', new Date().toISOString() + ' - ' + line + '\n', {flag: 'a+'}); originalConsole.apply(console, arguments); };
